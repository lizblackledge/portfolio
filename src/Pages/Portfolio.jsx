// import { Link } from "react-router-dom";
import { Typography, Container, GridList } from '@mui/material';

function Portfolio() {
  return (
    <Container maxWidth="md">
      <Typography variant="h1" color="primary.dark">
        Hi from Portfolio
      </Typography>
    </Container>
  );
}

export default Portfolio;